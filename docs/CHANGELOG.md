# 变更记录

## 2026-09-19 · v0.4.0 有限资金账户回放与增长曲线

- 新增 `finite-capital-v1` 确定性账户回放：以 1,000,000 USD 现金开局，总仓位上限 70%、单标的上限 12%、常规仓拆为六批；买卖各计 10 bps 滑点和 2 bps 费用。
- 只对本月窗口内能匹配到明确买入批次的卖单模拟成交；57 条期初遗留或无法匹配的卖单明确跳过，13 条待复核/短期重复信号不执行，4 条因资金或集中度上限跳过。
- 当前回放完成 148 次模拟买入和 203 次模拟卖出：期初权益 1,000,000 USD，期末估算权益 1,081,287.49 USD，账户增长率 +8.1287%，已实现盈亏 65,845.14 USD，未实现盈亏 15,442.35 USD，事件时点最大回撤 -2.3256%。
- 仍持有的 13 个模拟标的使用长桥最新可用盘前/盘后或常规盘报价估值；所有报价均成功返回，历史进出场价未被改写。
- 新增 4 张回放表：版本化运行、逐笔处理、事件时点权益快照和期末模拟持仓；每次运行只追加新版本，不覆盖旧结果。
- 网页改为账户增长率优先：新增动画权益曲线、买卖点、悬停数值、现金环、已实现/未实现盈亏、最大回撤、当前模拟持仓和精确买卖流水；公开 Pages 仅展示聚合曲线。
- 新增 5 项账户回放测试；完整测试 29/29 通过，PostgreSQL 迁移 001–004 完整，共 17 张公开表，应用角色与 Navicat 只读角色权限保持不变。

## 2026-09-19 · 网页展示名称调整

- 将仪表盘页眉中的“赵哥 / ZHAO”统一改为“咪哆 / MIDO”。
- 仅调整用户界面显示；Discord 原始消息、历史导入标签和解析前缀保持原样，确保审计链不被改写。

## 2026-09-19 · 一个月历史复盘与长桥行情卡

- 通过 Discord 官方 Bot 回补 2026-08-19 至 2026-09-19 的目标频道历史，共 629 条原始消息，包含 9 月 18 日晚间操作；原文只进入本机 PostgreSQL 和忽略目录，不进入 GitHub Pages。
- 新增历史导入批次、消息采集模式和策略交易腿表；导入按源文件 SHA-256 与 Discord Message ID 幂等，重复执行不会重复记账。
- 新增严格历史解析器 `history-rules-v1`：区分买入、加回和卖出；拒绝长篇举例中的数字误识别；同一卖出消息的多个明确成本批次拆为多个交易腿；重复转发不进入胜率。
- 严格自动口径得到 229 条已完成交易腿：211 盈、12 亏、6 持平，胜率 92.14%，单腿中位理论收益 2.84%，平均理论收益 3.84%；另有 67 条交易类消息待人工理解、7 条中文名称映射待复核。
- 上述收益是频道明确价格形成的“信号级理论收益”，不是有限资金组合收益，未计消息延迟、滑点、费用和持仓占用；网页显著展示该限制。
- 重做本地月度复盘网页：增加覆盖漏斗、每日活跃度、标的胜率、可筛选交易腿账本和原文查看；公开静态页在无本地后端时自动进入隐私模式，不附带付费频道数据。
- 增加只读长桥行情端点，展示高频标的常规盘及最新盘前/盘后价格和行情时间；行情只用于参考，不回写历史成交价。
- 验证：`npm test` 24/24 通过；数据库迁移 001–003 完整、应用角色仍为非超级用户、只读角色保持只读；历史导入第二次执行返回幂等；本地 API 返回 629 条原始消息、229 条已完成腿和 29 个标的；长桥 6 个高频标的报价成功。

## 2026-09-19 · StockRocks 权限与转发 Bot 兼容

- 实际验证 `duobot` 已加入目标服务器，可读取目标文字频道及最近历史消息，View Channel 与 Read Message History 权限生效。
- 发现目标发布者是 Discord 转发 Bot；原安全过滤会拒绝所有 Bot 消息，因此即使频道权限正确也会漏采。
- 调整为只接收订阅中明确列出的转发 Bot 用户 ID；频道通配仍只接收真人，并始终拒绝采集器自身消息。
- 增加明确允许、未列入允许名单和采集器自身三类自动测试；真实服务器、频道和发布者 ID 仍只保存在本机配置中。

## 2026-09-18 · 完整中文数据字典

- 将 11 张 PostgreSQL 表按端到端链路重新说明，逐项记录字段类型、含义、可空性和业务边界。
- 补充候选、审核和模拟订单的全部状态值，以及 `ambiguous` JSON 字段与网页驼峰字段映射。
- 补充资金利用率、股票权重、理论收益与可执行模拟收益等口径，明确解析置信度不代表盈利概率。
- 增加 Navicat 常用只读 SQL、表关联关系、当前为空属正常的待接入表，以及 10 个常见误解提醒。
- 文档不包含真实 Discord ID、数据库密码、Token 或连接串。

## 2026-09-18 · 规则解析器 v1.1 多动作安全闸门

- 真实消息同时包含 NVDA 减仓与另一个疑似拼写错误标的买入，暴露出 v1 只解析第一段却标记可执行的问题。
- v1.1 检测同一消息中的多个交易动作、多个或未验证的股票标识；此类消息强制 `ambiguous=true`、降低置信度并禁止自动执行。
- 原 v1 候选保留但状态改为 `rejected`，追加正式拒绝决定和审计记录；同一原始消息新增 v1.1 不可执行候选，原始 Discord 事件未修改。
- 新增混合动作、多个大写股票代码测试；全部自动测试增至 17 项。

## 2026-09-18 · PostgreSQL 本机持久化启用

- PostgreSQL 13.21 创建项目数据库 `swing_signal_desk`、应用角色 `swing_signal_app` 和 Navicat 只读角色 `swing_signal_readonly`；两者均为随机 256 位密码且仅保存在本机忽略文件。
- 应用角色不是超级用户，无创建数据库/角色/复制权限；只读角色默认事务只读，实际写入测试被 PostgreSQL `25006` 拒绝。
- 执行迁移 `001_initial_schema.sql`、`002_discord_subscriptions.sql`，安装 `pgcrypto 1.3`，创建 11 张业务/迁移表和默认 1,000,000 USD 模拟组合。
- 两条本机 Discord 订阅已写入数据库；真实 ID 未进入公开仓库。
- 服务从内存模式切换为 PostgreSQL；写入 `manual_test` 后重启，消息和候选信号仍存在。
- 真实 Discord 无结构测试消息约 0.324 秒写入 PostgreSQL；规则解析为 `note`、置信度 40%、不可执行，服务重启后消息与候选信号均仍存在。
- 统一 PostgreSQL 与内存仓库的 REST/SSE 驼峰字段结构，新增映射测试和 `npm run db:verify`。
- 新增 `npm run db:backup`：使用 `pg_dump` 自定义格式、生成 SHA-256 清单，并用 `pg_restore --list` 检查归档可读性；备份目录被 Git 忽略。
- 原 `pg_hba.conf` 已逐字节恢复，SHA-256 为 `844B54976378EA64CF9440355D8D637090712707F11E6BBF20C21C09CD75CB6A`；无密码 `postgres` 连接再次失败。
- 新增 `docs/POSTGRES_SETUP.md`，记录权限、Navicat、迁移、验证、备份边界和故障排查。

## 2026-09-18 · Discord 真实端到端验收

- `duobot` 已用最小只读权限加入用户控制的测试服务器；Bot Token 仅保存于被 Git 忽略的本机 `.env`，未进入聊天、日志或仓库。
- 实际登录验证成功，目标测试频道具备 View Channel 与 Read Message History；本地健康检查返回 Discord 已启用。
- 中文样本“减仓止盈半仓 NVDA 价格220”从 Discord 到本地记录约 0.215 秒，正确解析为 NVDA / sell / 50% / 220，置信度 100%，状态仍为 `pending_review`。
- 第二条样本约 0.196 秒进入本地；同一 Discord Message ID 的创建、编辑和删除分别追加为 `create`、`update`、`delete` 三条记录，创建版 221 与编辑版 222 均保留。
- 当前验收仍使用内存仓库，重启会清空本轮数据；只有 `create` 事件生成候选信号，编辑事件的候选修订/撤销逻辑仍待实现。
- 真实服务器 ID、频道 ID、作者 ID 和 Token 仅保存在本机配置或运行数据中，不写入公开文档。

## 2026-09-18 · Discord 应用配置

- 在 Discord Developer Portal 创建官方 Bot `duobot`。
- 已开启 Message Content Intent；Presence Intent 与 Server Members Intent 保持关闭。
- 固定最小邀请权限为 View Channel + Read Message History（权限整数 `66560`），不授予发言或管理员权限。
- 增加 Application ID、最小权限邀请、私密频道权限覆盖和 Token 保管的操作说明。
- 当前仍未启用真实采集：等待服务器管理员完成邀请，以及用户本人把 Token 写入被 Git 忽略的本机环境变量。
- 修改前验证：`npm ci` 成功，`npm test` 12/12 通过，依赖审计为 0 个漏洞。

## 2026-09-18 · Discord 精确范围过滤

- 增加 `DISCORD_GUILD_ID` 与 `DISCORD_AUTHOR_IDS` 配置。
- 消息必须同时匹配服务器、频道、发布者，并且发布者不能是 Bot，才会进入留档和解析流程。
- 增加多订阅配置和 `discord_subscriptions` 数据表；每个频道可独立配置多个发布者，也可监听该频道全部非 Bot 用户。
- 真实 Discord ID 仅保存在被 Git 忽略的本机 `.env`，不进入公开仓库。

## 2026-09-18 · v0.2.0 基础后端

- 增加 Node.js API、SSE、内存仓库和 PostgreSQL 仓库。
- 增加 PostgreSQL 初始迁移，覆盖消息事件、解析、人工决定、行情、模拟订单、成交、持仓快照和审计。
- 增加中文交易指令规则解析器和资金风控。
- 增加官方 Discord Bot 采集骨架，保留 create/update/delete 事件。
- 增加 OpenAI Responses API 严格结构化输出适配器；默认关闭，不包含密钥或默认模型。
- 增加项目上下文、数据字典、安全、运行与路线图文档。
- 本地验证：`npm test` 8/8 通过；`/api/health` 返回 v0.2.0、memory；示例“减仓止盈半仓 NVDA 价格220”解析为 NVDA / sell / 50%，并保留人工确认闸门。
- GitHub 验证：基础提交 `f402bcd` 的 CI 与 Pages 工作流均成功；线上页面返回 HTTP 200，并包含后端状态探测代码。

## 2026-09-18 · v0.1.0 静态原型

- 完成波段信号、持仓、资金利用率、交易记录和收益复盘的动画网页演示。
- 使用 GitHub Pages 发布公开网址。
